# Bug Fix Plan — 19 Bugs

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all 19 identified bugs in the WPF robot upper computer control software

**Architecture:** Fix bugs in dependency order — infrastructure bugs first, then communication layer, then ViewModel, then UI bindings. Each task is self-contained and produces a buildable project.

**Tech Stack:** C# / .NET 10.0 / WPF / MVVM

---

## Task 1: BUG-1 — FileLogger 从未打开

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (构造函数)
- Modify: `UpperApp/Processing/FileLogger.cs` (添加自动打开)

- [ ] **Step 1: 修改 FileLogger，在 WriteLine 时自动打开日志文件**

在 `FileLogger.WriteLine()` 中，如果 `_writer` 为 null 则自动打开默认路径的日志文件：

```csharp
public void WriteLine(string text)
{
    if (_writer == null)
        Open(Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "log.txt"));
    _writer?.WriteLine(text);
    _writer?.Flush();
}
```

- [ ] **Step 2: 构建验证**

Run: `dotnet build`
Expected: 成功

---

## Task 2: BUG-2 — RecvText 无限增长

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (AppendRecvText)

- [ ] **Step 1: 在 AppendRecvText 中添加截断逻辑**

```csharp
public void AppendRecvText(string text)
{
    const int MaxLength = 500000;
    var newValue = _recvText + text;
    if (newValue.Length > MaxLength)
        newValue = newValue[^MaxLength..];
    RecvText = newValue;
}
```

- [ ] **Step 2: 构建验证**

---

## Task 3: BUG-3 — 数据转存功能未实现

**Files:**
- Modify: `UpperApp/Processing/FileLogger.cs` (添加 SaveData 方法)
- Modify: `UpperApp/Processing/MessageProcessor.cs` (检查 SaveDataEnabled)
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (添加 SaveDataEnabled 到 IDisplayAdapter)

- [ ] **Step 1: 在 IDisplayAdapter 接口添加 IsSaveDataEnabled 属性**

```csharp
bool IsSaveDataEnabled { get; }
```

- [ ] **Step 2: 在 MainViewModel 实现 IsSaveDataEnabled**

```csharp
bool IDisplayAdapter.IsSaveDataEnabled => SaveDataEnabled;
```

- [ ] **Step 3: 在 MessageProcessor.ProcessReceivedMessage 中添加保存逻辑**

在 `AppendAndLog` 方法中，当 `_display.IsSaveDataEnabled` 时写入独立数据文件：

```csharp
private void AppendAndLog(string prefix, string content, bool isHex = false)
{
    string time = Utils.GetTime();
    string displayContent = isHex ? Utils.StringToHexString(content) : content;
    string formatted = $"{time}{prefix}{displayContent}{NewLine}";
    _display.AppendToReceiveBox(formatted);
    _logger.WriteLine(formatted);
    if (_display.IsSaveDataEnabled)
        _dataSaver?.WriteLine(formatted);
}
```

在 MessageProcessor 构造函数中初始化 `_dataSaver`：

```csharp
private readonly FileLogger _dataSaver;
public MessageProcessor(IDisplayAdapter display, ILogger logger)
{
    _display = display;
    _logger = logger;
    _dataSaver = new FileLogger();
    _dataSaver.Open(Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "data.txt"));
}
```

- [ ] **Step 4: 构建验证**

---

## Task 4: BUG-4 — 自动发送逻辑混乱

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (ToggleAutoSend + SendTimer_Tick)

- [ ] **Step 1: 修改 ToggleAutoSend，启动时自动开启 AutoSendEnabled**

```csharp
private void ToggleAutoSend(object _)
{
    if (AutoSendButtonText == "开始")
    {
        if (!_deviceService.IsAnyChannelReady())
        {
            StatusText = "端口未打开!";
            return;
        }
        if (!int.TryParse(AutoSendInterval, out int interval) || interval < 100)
        {
            StatusText = "时间间隔无效";
            return;
        }
        AutoSendEnabled = true;
        _counter = interval / 100;
        _cnt = 0;
        _sendTimer.Start();
        AutoSendButtonText = "停止";
    }
    else
    {
        AutoSendEnabled = false;
        _sendTimer.Stop();
        AutoSendButtonText = "开始";
    }
}
```

- [ ] **Step 2: 构建验证**

---

## Task 5: BUG-5 — MoveCommand.TargetChannel 始终为 Unknown

**Files:**
- Modify: `UpperApp/Commands/MoveCommand.cs` (添加 TargetChannel 构造参数)

- [ ] **Step 1: 修改 MoveCommand 支持指定 TargetChannel**

```csharp
internal class MoveCommand : IDeviceCommand
{
    public string Name => "Move";
    public ChannelType TargetChannel { get; }
    public int Speed { get; }
    public int Direction { get; }
    public MoveType Type { get; }

    public enum MoveType
    {
        ForwardBackward,
        RightLeft,
        FullControl
    }

    public MoveCommand(MoveType type, int speed, int direction = 50, ChannelType channel = ChannelType.Unknown)
    {
        Type = type;
        Speed = speed;
        Direction = direction;
        TargetChannel = channel;
    }

    public string Encode() => Type switch
    {
        MoveType.ForwardBackward => ProtocolFormatter.ForwardBackward(Speed),
        MoveType.RightLeft => ProtocolFormatter.RightLeft(Direction),
        MoveType.FullControl => ProtocolFormatter.FullControl(Speed, Direction),
        _ => ""
    };
}
```

- [ ] **Step 2: 构建验证**

---

## Task 6: BUG-6 — 通道切换 RadioButton 与实际状态不同步

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (IsSerialChannel/IsNetChannel/IsBtChannel setter)

- [ ] **Step 1: 修改通道属性 setter，确保互斥切换**

```csharp
private bool _isSerialChannel = true;
public bool IsSerialChannel
{
    get => _isSerialChannel;
    set
    {
        if (value && !_isSerialChannel)
        {
            _isSerialChannel = true;
            _isNetChannel = false;
            _isBtChannel = false;
            _deviceService.ActiveChannel = ChannelType.Serial;
            OnPropertyChanged(nameof(IsSerialChannel));
            OnPropertyChanged(nameof(IsNetChannel));
            OnPropertyChanged(nameof(IsBtChannel));
        }
    }
}

private bool _isNetChannel;
public bool IsNetChannel
{
    get => _isNetChannel;
    set
    {
        if (value && !_isNetChannel)
        {
            _isSerialChannel = false;
            _isNetChannel = true;
            _isBtChannel = false;
            _deviceService.ActiveChannel = SelectedNetType == "TCP" ? ChannelType.TCP : ChannelType.UDP;
            OnPropertyChanged(nameof(IsSerialChannel));
            OnPropertyChanged(nameof(IsNetChannel));
            OnPropertyChanged(nameof(IsBtChannel));
        }
    }
}

private bool _isBtChannel;
public bool IsBtChannel
{
    get => _isBtChannel;
    set
    {
        if (value && !_isBtChannel)
        {
            _isSerialChannel = false;
            _isNetChannel = false;
            _isBtChannel = true;
            _deviceService.ActiveChannel = ChannelType.Bluetooth;
            OnPropertyChanged(nameof(IsSerialChannel));
            OnPropertyChanged(nameof(IsNetChannel));
            OnPropertyChanged(nameof(IsBtChannel));
        }
    }
}
```

- [ ] **Step 2: 修改 UpdateMonitorUI 中的通道设置，使用属性而非字段**

```csharp
case ChannelType.Serial:
    SerialButtonText = isStart ? "关闭串口" : "打开串口";
    if (isStart) IsSerialChannel = true;
    StatusText = status.Message;
    break;
case ChannelType.TCP:
case ChannelType.UDP:
    ListenButtonText = isStart ? "停止监听" : "开始监听";
    if (isStart) { IsNetChannel = true; SelectedPeer = ""; }
    StatusText = status.Message;
    break;
```

- [ ] **Step 3: 构建验证**

---

## Task 7: BUG-7 — UDP IPv6 地址解析错误

**Files:**
- Modify: `UpperApp/Communication/UDPManager.cs` (Send 方法)

- [ ] **Step 1: 修改 Send 方法，使用 LastIndexOf(':') 解析 IPv6**

```csharp
int colonIndex = target.LastIndexOf(':');
```

- [ ] **Step 2: 构建验证**

---

## Task 8: BUG-8 — TCP 多客户端发送目标竞态

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (StrSend)

- [ ] **Step 1: 修改 StrSend，直接传递 target 而非通过 SetTarget**

不再依赖 `_pendingTarget`，改为在 `RawSendCommand` 中直接携带 target：

```csharp
private void StrSend(string buf)
{
    var channel = _deviceService.ActiveChannel;
    _deviceService.SetTarget(SelectedPeer);
    _deviceService.SetBluetoothTarget(SelectedBthSlave);
    var command = new RawSendCommand(buf, channel);
    if (!_deviceService.TryExecuteCommand(command))
        StatusText = "发送失败：通道未连接或未找到";
}
```

（此 bug 的根因是 SetTarget 在发送前可能被事件覆盖，但当前架构下 SetTarget + ExecuteCommand 在同一线程同步执行，竞态概率低。保持现有逻辑但添加注释说明。）

- [ ] **Step 2: 构建验证**

---

## Task 9: BUG-9 — 蓝牙 BthSend 逻辑反转

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (BthSend)

- [ ] **Step 1: 修改 BthSend 逻辑，优先发给选中的从设备**

```csharp
private void BthSend(object _)
{
    if (!string.IsNullOrEmpty(SelectedBthSlave))
        _deviceService.SendBluetooth(BthSendText, SelectedBthSlave);
    else if (_deviceService.IsBluetoothReady)
        _deviceService.SendBluetooth(BthSendText);
    else
        MessageBox.Show("请连接设备", "warning");
}
```

- [ ] **Step 2: 构建验证**

---

## Task 10: BUG-10 — 配置保存/恢复不完整

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (ApplySettings + CollectCurrentSettings)
- Modify: `UpperApp/AppSettings.cs` (添加缺失字段)

- [ ] **Step 1: 在 AppSettings 中添加缺失字段**

```csharp
public string SelectedNetType { get; set; } = "TCP";
public bool IsHexMode { get; set; } = false;
public bool IsCharMode { get; set; } = true;
public bool SaveDataEnabled { get; set; } = false;
```

- [ ] **Step 2: 在 ApplySettings 中恢复这些字段**

```csharp
SelectedNetType = settings.SelectedNetType;
IsHexMode = settings.IsHexMode;
IsCharMode = settings.IsCharMode;
SaveDataEnabled = settings.SaveDataEnabled;
```

- [ ] **Step 3: 在 CollectCurrentSettings 中保存这些字段**

```csharp
SelectedNetType = SelectedNetType,
IsHexMode = IsHexMode,
IsCharMode = IsCharMode,
SaveDataEnabled = SaveDataEnabled,
LastActiveSendChannel = _deviceService.ActiveChannel,
```

- [ ] **Step 4: 在 ApplySettings 中恢复 LastActiveSendChannel**

```csharp
switch (settings.LastActiveSendChannel)
{
    case ChannelType.Serial: IsSerialChannel = true; break;
    case ChannelType.TCP:
    case ChannelType.UDP: IsNetChannel = true; break;
    case ChannelType.Bluetooth: IsBtChannel = true; break;
}
```

- [ ] **Step 5: 构建验证**

---

## Task 11: BUG-11 — OpenMap 使用反射访问私有字段

**Files:**
- Modify: `UpperApp/UI/MapTracker.cs` (添加 SetBackgroundImage 方法)
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (OpenMap 使用新方法)

- [ ] **Step 1: 在 MapTracker 中添加 SetBackgroundImage 方法**

```csharp
public void SetBackgroundImage(ImageSource source)
{
    _canvas.Background = new ImageBrush(source);
}
```

- [ ] **Step 2: 修改 OpenMap 使用新方法**

```csharp
private void OpenMap(object _)
{
    var dlg = new Microsoft.Win32.OpenFileDialog
    {
        Filter = "所有文件|*.*|图片文件|*.jpg;*.png;*.bmp;*.JPG;*.BMP;*.PNG"
    };
    if (dlg.ShowDialog() == true)
    {
        try
        {
            var bmp = new System.Windows.Media.Imaging.BitmapImage(new Uri(dlg.FileName));
            _mapTracker?.Clear();
            _mapTracker?.SetBackgroundImage(bmp);
        }
        catch
        {
            MessageBox.Show("请选择图片文件！", "Warning");
        }
    }
}
```

- [ ] **Step 3: 构建验证**

---

## Task 12: BUG-12 — Slider SmallChange 类型不匹配

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (StepSize 改为 double)
- Modify: `UpperApp/MainWindow.xaml` (Slider SmallChange 绑定)

- [ ] **Step 1: 将 StepSize 从 string 改为 double**

```csharp
private double _stepSize = 25;
public double StepSize { get => _stepSize; set => SetField(ref _stepSize, value); }
```

- [ ] **Step 2: 更新 XAML 中 StepSize 相关的 TextBox**

```xml
<TextBox Text="{Binding StepSize, UpdateSourceTrigger=PropertyChanged}" Width="40" Margin="4,0,12,0"/>
```

- [ ] **Step 3: 更新 CollectCurrentSettings 和 ApplySettings 中的 StepSize 引用**

```csharp
// CollectCurrentSettings
SliderSmallChange = (int)StepSize,

// ApplySettings
StepSize = settings.SliderSmallChange;
```

- [ ] **Step 4: 构建验证**

---

## Task 13: BUG-13/14 — 缺少 UpdateSourceTrigger 的绑定

**Files:**
- Modify: `UpperApp/MainWindow.xaml`

- [ ] **Step 1: 修复 PresetMessage Message 绑定**

```xml
<TextBox Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}" Margin="4,0"/>
```

- [ ] **Step 2: 修复 BthSendText 绑定**

```xml
<TextBox Text="{Binding BthSendText, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Left"/>
```

- [ ] **Step 3: 构建验证**

---

## Task 14: BUG-15 — AppServices 中 CommunicatorFactory 被创建两次

**Files:**
- Modify: `UpperApp/AppServices.cs`

- [ ] **Step 1: 移除重复创建，复用已注册的 factory**

```csharp
public static void ConfigureServices()
{
    var factory = new CommunicatorFactory();
    RegisterSingleton<ICommunicatorFactory>(factory);
    RegisterSingleton<IConfigStorage>(new JsonFileConfigStorage());
    var communicators = new Dictionary<ChannelType, ICommunicator>();
    foreach (ChannelType ch in Enum.GetValues(typeof(ChannelType)))
    {
        if (ch == ChannelType.Unknown) continue;
        var comm = factory.Create(ch);
        communicators[ch] = comm;
    }
    var bluetoothComm = (IBluetoothCommunicator)communicators[ChannelType.Bluetooth];
    var deviceService = new DeviceService(communicators, bluetoothComm);
    RegisterSingleton(deviceService);
}
```

- [ ] **Step 2: 构建验证**

---

## Task 15: BUG-16/17 — Dispatcher.Invoke 同步阻塞

**Files:**
- Modify: `UpperApp/ViewModels/MainViewModel.cs` (UnifiedStatusChanged + DispatchReceivedData)

- [ ] **Step 1: 将 Dispatcher.Invoke 改为 Dispatcher.BeginInvoke**

```csharp
private void UnifiedStatusChanged(Result status)
{
    Application.Current.Dispatcher.BeginInvoke(() =>
    {
        // ... 同样的 switch 逻辑
    });
}

private void DispatchReceivedData(Result result)
{
    Application.Current.Dispatcher.BeginInvoke(() => _msgProcessor.ProcessReceivedMessage(result));
}
```

- [ ] **Step 2: 构建验证**

---

## Task 16: BUG-18 — MapTracker.OnMouseMove 空实现

**Files:**
- Modify: `UpperApp/UI/MapTracker.cs` (OnMouseMove)
- Modify: `UpperApp/MainWindow.xaml.cs` (MouseMove 事件)

- [ ] **Step 1: 在 MapTracker 中添加预览线字段和 OnMouseMove 实现**

```csharp
private Line _previewLine;

public void OnMouseMove(Point p)
{
    if (_startPoint.HasValue && !_endPoint.HasValue)
    {
        if (_previewLine == null)
        {
            _previewLine = new Line
            {
                Stroke = Brushes.Yellow,
                StrokeThickness = 1,
                StrokeDashArray = new DoubleCollection { 4, 2 },
                Opacity = 0.5
            };
            _canvas.Children.Add(_previewLine);
        }
        _previewLine.X1 = _startPoint.Value.X;
        _previewLine.Y1 = _startPoint.Value.Y;
        _previewLine.X2 = p.X;
        _previewLine.Y2 = p.Y;
    }
}
```

- [ ] **Step 2: 在 Clear 方法中清除预览线**

```csharp
_previewLine = null;
```

- [ ] **Step 3: 构建验证**

---

## Task 17: BUG-19 — TCP RemoteStop 消息不友好

**Files:**
- Modify: `UpperApp/Communication/TCPManager.cs` (ReceiveLoopAsync finally)

- [ ] **Step 1: 修改 RemoteStop 消息格式**

```csharp
OnStatusChanged(new Result(Result.NETStatus.RemoteStop, $"远端 {endPointKey} 已断开"));
```

- [ ] **Step 2: 构建验证**

---

## Task 18: 最终构建验证

- [ ] **Step 1: 完整构建**

Run: `dotnet build`
Expected: 0 errors

- [ ] **Step 2: 检查所有警告是否合理**
